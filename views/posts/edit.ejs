<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Edit Post</title>

    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css"
    />

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      defer
    ></script>
  </head>
  <body>
    <%- include('../partials/header') %>

    <main class="container my-4">
      <div class="card shadow-sm rounded-0 mx-auto" style="max-width: 600px">
        <div class="card-header bg-white border-0">
          <h3>Edit Post</h3>
          <small class="text-muted"
            >Posted by <strong><%= post.user.username %></strong></small
          >
        </div>

        <div class="card-body">
          <div class="mb-3">
            <img
              src="/uploads/<%= post.picture %>"
              alt="Post Image"
              class="img-fluid d-block mb-3"
              style="max-height: 300px; object-fit: cover; width: 100%"
            />
          </div>
          <form
            action="/posts/<%= post._id %>?_method=PUT"
            method="POST"
            enctype="multipart/form-data"
          >
            <div class="mb-3">
              <label for="description" class="form-label">Description</label>
              <textarea
                id="description"
                name="description"
                class="form-control"
                rows="4"
                value="<%= post.description %>"
              >
              </textarea>
            </div>

            <button type="submit" class="btn btn-dark">Save Changes</button>
            <a href="/posts/<%= post._id %>" class="btn btn-secondary ms-2"
              >Cancel</a
            >
          </form>
        </div>
      </div>
    </main>
  </body>
</html>
